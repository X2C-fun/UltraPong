[package]
name = "ultrapong-physics"
version = "0.1.0"
edition = "2021"
[lib]
crate-type = ["cdylib", "rlib"]
[dependencies]
borsh = { version = "1.6.0", features = ["derive"] }
serde = { version = "1", features = ["derive"] }
serde_json = "1"

